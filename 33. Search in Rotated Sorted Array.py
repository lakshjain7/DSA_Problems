"""\nProblem #33 — Search in Rotated Sorted Array\nDifficulty : Medium\nTopics     : Binary Search, Array\n\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nPROBLEM STATEMENT\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nThere is an integer array nums sorted in ascending order (with distinct values).\nPrior to being passed to your function, nums is possibly rotated at an unknown\npivot index k (1 <= k < nums.length) such that the resulting array is\n[nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]].\n\nFor example, [0,1,2,4,5,6,7] might be rotated at pivot index 3 and become\n[4,5,6,7,0,1,2].\n\nGiven the rotated array nums and an integer target, return the index of target\nif it is in nums, or -1 if it is not in nums.\n\nYou must write an algorithm with O(log n) runtime complexity.\n\nExamples:\n  Input: nums = [4,5,6,7,0,1,2], target = 0  →  Output: 4\n  Input: nums = [4,5,6,7,0,1,2], target = 3  →  Output: -1\n  Input: nums = [1], target = 0               →  Output: -1\n\nConstraints:\n  1 <= nums.length <= 5000\n  -10^4 <= nums[i] <= 10^4\n  All values of nums are unique.\n  nums is an ascending array that is possibly rotated.\n  -10^4 <= target <= 10^4\n\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nAPPROACH — Modified Binary Search\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nKey insight: even after rotation, one of the two halves [lo..mid] or [mid..hi]\nis always sorted. We can determine which half is sorted by comparing nums[lo]\nwith nums[mid]:\n\n1. If nums[lo] <= nums[mid]:  the LEFT half is sorted.\n   - If target is in [nums[lo], nums[mid])  →  search left\n   - Otherwise                              →  search right\n\n2. Else: the RIGHT half is sorted.\n   - If target is in (nums[mid], nums[hi]]  →  search right\n   - Otherwise                              →  search left\n\nThis maintains O(log n) by halving the search space each iteration.\n\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nCOMPLEXITY\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nTime  : O(log n)  — binary search\nSpace : O(1)      — iterative, no extra data structures\n"""\n\nfrom typing import List\n\n\ndef search(nums: List[int], target: int) -> int:\n    \"\"\"Return index of target in rotated sorted array, or -1 if absent.\"\"\"\n    lo, hi = 0, len(nums) - 1\n\n    while lo <= hi:\n        mid = (lo + hi) // 2\n\n        if nums[mid] == target:\n            return mid\n\n        # Left half is sorted\n        if nums[lo] <= nums[mid]:\n            if nums[lo] <= target < nums[mid]:\n                hi = mid - 1          # target in left sorted portion\n            else:\n                lo = mid + 1          # target must be in right portion\n        # Right half is sorted\n        else:\n            if nums[mid] < target <= nums[hi]:\n                lo = mid + 1          # target in right sorted portion\n            else:\n                hi = mid - 1          # target must be in left portion\n\n    return -1\n\n\n# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n# ALTERNATIVE APPROACH — Find Pivot First, Then Binary Search\n# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n# Step 1: binary-search for the rotation pivot (minimum element index).\n# Step 2: determine which sorted half the target falls in, then standard\n#         binary search within that half.\n# Same O(log n) time but requires two passes; the single-pass approach above\n# is cleaner for interview contexts.\n\ndef search_two_pass(nums: List[int], target: int) -> int:\n    \"\"\"Two-pass: find pivot, then binary search in the correct half.\"\"\"\n    n = len(nums)\n\n    # Find index of minimum element (the rotation pivot)\n    lo, hi = 0, n - 1\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if nums[mid] > nums[hi]:\n            lo = mid + 1\n        else:\n            hi = mid\n    pivot = lo  # index of smallest element\n\n    # Decide which half to search\n    if nums[pivot] <= target <= nums[n - 1]:\n        lo, hi = pivot, n - 1\n    else:\n        lo, hi = 0, pivot - 1\n\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if nums[mid] == target:\n            return mid\n        elif nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n\n    return -1\n\n\nif __name__ == \"__main__\":\n    # Basic examples from problem statement\n    assert search([4, 5, 6, 7, 0, 1, 2], 0) == 4\n    assert search([4, 5, 6, 7, 0, 1, 2], 3) == -1\n    assert search([1], 0) == -1\n\n    # Target at boundaries\n    assert search([4, 5, 6, 7, 0, 1, 2], 4) == 0   # first element\n    assert search([4, 5, 6, 7, 0, 1, 2], 2) == 6   # last element\n\n    # No rotation\n    assert search([1, 2, 3, 4, 5], 3) == 2\n\n    # Two-element arrays\n    assert search([3, 1], 1) == 1\n    assert search([3, 1], 3) == 0\n    assert search([3, 1], 2) == -1\n\n    # Target is the pivot element\n    assert search([6, 7, 1, 2, 3, 4, 5], 1) == 2\n    assert search([6, 7, 1, 2, 3, 4, 5], 6) == 0\n\n    # Alternative implementation matches\n    for nums, target in [\n        ([4, 5, 6, 7, 0, 1, 2], 0),\n        ([4, 5, 6, 7, 0, 1, 2], 3),\n        ([6, 7, 1, 2, 3, 4, 5], 5),\n        ([1], 1),\n    ]:\n        assert search(nums, target) == search_two_pass(nums, target)\n\n    print(\"All assertions passed!\")\n