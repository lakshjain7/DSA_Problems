"""\nProblem Number: 153\nTitle: Find Minimum in Rotated Sorted Array\nDifficulty: Medium\nTopics: Array, Binary Search\n\nProblem Statement:\n------------------\nSuppose an array of length n sorted in ascending order is rotated between\n1 and n times. For example, the array nums = [0,1,2,4,5,6,7] might become:\n  [4,5,6,7,0,1,2] if rotated 4 times, or\n  [0,1,2,4,5,6,7] if rotated 7 times (back to original).\n\nNotice that rotating an array [a[0], a[1], ..., a[n-1]] 1 time results in\nthe array [a[n-1], a[0], a[1], ..., a[n-2]].\n\nGiven the sorted rotated array nums of unique elements, return the minimum\nelement of this array. You must write an algorithm that runs in O(log n) time.\n\nExamples:\n---------\nInput: nums = [3, 4, 5, 1, 2]\nOutput: 1\n\nInput: nums = [4, 5, 6, 7, 0, 1, 2]\nOutput: 0\n\nInput: nums = [11, 13, 15, 17]\nOutput: 11\n\nConstraints:\n------------\n- n == nums.length\n- 1 <= n <= 5000\n- -5000 <= nums[i] <= 5000\n- All integers in nums are unique.\n- nums is sorted and rotated between 1 and n times.\n\nApproach:\n---------\nModified Binary Search.\n\nThe key insight is that a rotated sorted array is made of two sorted\n\"halves\" separated at the rotation point. The minimum is the first element\nof the right half (i.e., the element at the rotation pivot).\n\nDecision rule at midpoint `mid`:\n  - If nums[mid] > nums[right]:\n      The minimum is in the RIGHT half (mid+1 .. right).\n      (The left half is sorted and all its values > nums[right] > min.)\n  - Else (nums[mid] <= nums[right]):\n      The minimum is in the LEFT half including mid (left .. mid).\n      (`mid` itself might be the minimum.)\n\nWe narrow [left, right] until left == right, which is the minimum.\n\nWhy it works:\nWe always compare mid against the rightmost element. If mid's value is\nlarger than the rightmost, we know the pivot (minimum) is to the right of\nmid. Otherwise the pivot is at mid or to the left.\n\nComplexity Analysis:\n--------------------\nTime  : O(log n) — binary search halves the range each iteration\nSpace : O(1)     — constant extra space\n\nAlternative Approach:\n---------------------\nLinear scan O(n): iterate and track minimum — simple but doesn't meet the\nO(log n) requirement.\n"""\n\nfrom typing import List\n\n\ndef findMin(nums: List[int]) -> int:\n    """Return the minimum element of a sorted rotated array.\n\n    Args:\n        nums: A sorted array rotated between 1 and n times, with unique values.\n\n    Returns:\n        The minimum integer in the array.\n    """\n    left, right = 0, len(nums) - 1\n\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] > nums[right]:\n            # Minimum is strictly to the right of mid\n            left = mid + 1\n        else:\n            # Minimum is at mid or to the left of mid\n            right = mid\n\n    return nums[left]\n\n\n# ---------------------------------------------------------------------------\n# Alternative: linear scan (O(n), for reference)\n# ---------------------------------------------------------------------------\ndef findMin_linear(nums: List[int]) -> int:\n    \"\"\"O(n) fallback using a simple linear scan.\"\"\"\n    return min(nums)\n\n\n# ---------------------------------------------------------------------------\n# Tests\n# ---------------------------------------------------------------------------\nif __name__ == \"__main__\":\n    # Examples from the problem statement\n    assert findMin([3, 4, 5, 1, 2]) == 1\n    assert findMin([4, 5, 6, 7, 0, 1, 2]) == 0\n    assert findMin([11, 13, 15, 17]) == 11   # no rotation\n\n    # Single element\n    assert findMin([1]) == 1\n\n    # Two elements, rotated\n    assert findMin([2, 1]) == 1\n\n    # Two elements, not rotated\n    assert findMin([1, 2]) == 1\n\n    # Minimum at the very end (rotated once)\n    assert findMin([2, 3, 4, 5, 1]) == 1\n\n    # Minimum at the beginning (not rotated)\n    assert findMin([1, 2, 3, 4, 5]) == 1\n\n    # Minimum in the middle\n    assert findMin([5, 6, 7, 1, 2, 3, 4]) == 1\n\n    # Negative numbers\n    assert findMin([0, 1, 2, -4, -3, -2, -1]) == -4\n\n    # Verify consistency with linear scan\n    test_cases = [\n        [3, 4, 5, 1, 2],\n        [4, 5, 6, 7, 0, 1, 2],\n        [11, 13, 15, 17],\n        [2, 1],\n        [5, 6, 7, 1, 2, 3, 4],\n    ]\n    for tc in test_cases:\n        assert findMin(tc) == findMin_linear(tc)\n\n    print(\"All tests passed!\")\n